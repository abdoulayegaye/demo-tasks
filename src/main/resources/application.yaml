spring:
  application:
    name: task-demo
  datasource:
    url: jdbc:mysql://localhost:3306/tasks_demo_db?useSSL=false&serverTimezone=UTC
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8081/realms/demo-realm
      client:
        registration:
          keycloak:
            client-id: springboot-api
            client-secret: 6eeuSfwukCpXYrU1u2DySfKRFnuM2FDJ
            authorization-grant-type: authorization_code
            redirect-uri: http://localhost:8080/login/oauth2/code/keycloak
        provider:
          keycloak:
            issuer-uri: http://localhost:8081/realms/demo-realm

server:
  port: 9090
